{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "types": ["vite/client"],
    "baseUrl": ".", // 👈 確保 paths 可以正常解析
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "env.d.ts",       // 👈 這個建議打開，不要註解掉
    "src/**/*.ts",
    "src/**/*.d.ts",
    "src/**/*.vue"
  ],
  "exclude": ["src/**/__tests__/*"]
}

